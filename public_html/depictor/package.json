{
  "private": true,
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": ">=22.0.0 <23.0.0"
  },
  "scripts": {
    "build": "vite build --mode development",
    "dist": "npm run update-locales && vite build --mode production",
    "postinstall": "npm run build",
    "update-locales": "node etc/update-locales.js",
    "watch": "vite build --mode development --watch"
  },
  "dependencies": {
    "donot": "^0.4.1",
    "sass": "^1.62.1",
    "valenski": "^1.5.0",
    "vue": "^2.7.14",
    "vue-i18n": "^8.28.2",
    "vuex": "^3.6.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue2": "^2.3.4",
    "terser": "^5.36.0",
    "vite": "^6.0.0"
  }
}